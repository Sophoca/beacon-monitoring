import React from 'react';
import Beacon from './Beacon';

const BeaconLayout = ({ beaconInfo }) => {
    return <div style={{ position: 'absoloute', zIndex: '1' }}></div>;
};

export default BeaconLayout;
